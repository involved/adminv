<% if target.errors.any? %>  
	<div class="message errormsg">  
		<p><%= pluralize(target.errors.count, "error") %> prohibited this <%= type %> from being saved:</p>
		<ul>  
			<% target.errors.full_messages.each do |msg| %>  
			<li><%= msg %></li>  
			<% end %>  
		</ul>  
	</div> <!-- .message.errormsg -->
<% end %>